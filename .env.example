# AI Lab - Environment Variables Example
# Copy this file to .env and fill in the values

# Database (SQLite for MVP)
DATABASE_URL="file:./dev.db"

# REQUIRED: Encryption key for API keys (32 bytes = 64 hex characters)
# Generate with: openssl rand -hex 32
APP_ENCRYPTION_KEY="your-64-character-hex-key-here"

# REQUIRED: JWT secret for session tokens
# Generate with: openssl rand -hex 32
JWT_SECRET="your-64-character-hex-key-here"
